import os
from setuptools import setup, find_packages

DEPENDENCIES = [
]

setup(
    name = "package",
    version = "0.0.0",
    author='Me',
    packages=find_packages(),
    install_requires=DEPENDENCIES,
)